# Copy this file to .env and fill in real values.
# Environment variables used by the Google Classroom API client

# Core Classroom configuration
# Default course ID to avoid passing --course-id everywhere
COURSE_ID=

# OAuth 2.0 (choose ONE auth method: credentials.json OR env vars below)
# If you place a downloaded OAuth client file as credentials.json you can leave these empty.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Redirect URI registered in Google Cloud Console (must match). For local dev usually http://localhost:8080/callback
GOOGLE_REDIRECT_URI=http://localhost:8080/callback

# Optional: override scopes (comma-separated). If unset, the client uses a sensible default list.
# Only include VALID Google Classroom / Drive scopes. Example:
# GOOGLE_SCOPES=https://www.googleapis.com/auth/classroom.courses,https://www.googleapis.com/auth/classroom.rosters,https://www.googleapis.com/auth/classroom.coursework.students,https://www.googleapis.com/auth/classroom.coursework.me,https://www.googleapis.com/auth/classroom.profile.emails,https://www.googleapis.com/auth/classroom.profile.photos,https://www.googleapis.com/auth/classroom.push-notifications,https://www.googleapis.com/auth/drive
GOOGLE_SCOPES=

# Service Account (optional alternative auth path). If you want to use a service account,
# normally you just place credentials.json. The legacy client_old.py also supports injecting
# the JSON fields via environment variables below (NOT recommended for production):
GOOGLE_PROJECT_ID=
GOOGLE_PRIVATE_KEY_ID=
# NOTE: Private keys often contain newlines. Replace literal newlines with \n if setting inline.
GOOGLE_PRIVATE_KEY=
GOOGLE_CLIENT_EMAIL=
GOOGLE_CLIENT_X509_CERT_URL=

# End of file
